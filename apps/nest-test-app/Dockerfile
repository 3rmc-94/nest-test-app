# This file is generated by Nx.
#
# Build the docker image with `npx nx docker-build nest-test-app`.
# Tip: Modify "docker-build" options in project.json to change docker build args.
#
# Run the container with `docker run -p 3000:3000 -t nest-test-app`.
FROM docker.io/node:lts-alpine

ENV HOST=0.0.0.0
ENV PORT=3000

WORKDIR /app

RUN addgroup --system nest-test-app && \
          adduser --system -G nest-test-app nest-test-app

COPY apps/nest-test-app/dist nest-test-app/
RUN chown -R nest-test-app:nest-test-app .

# You can remove this install step if you build with `--bundle` option.
# The bundled output will include external dependencies.
RUN npm --prefix nest-test-app --omit=dev -f install

CMD [ "node", "nest-test-app" ]
